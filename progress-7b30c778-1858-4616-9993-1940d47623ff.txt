# Feature-Dev Progress: Student System

## Completed Stories
- S01: 项目脚手架+数据库Schema ✅
- S02: 后端实体类+Mapper+级联删除 ✅
- S03: AUTH后端:统一登录+JWT+修改密码 ✅
- S04: STU后端:学生CRUD+模糊查询 ✅
- S05: TCH后端:教师CRUD ✅
- S06: CRS后端:课程CRUD ✅
- S07: OPN后端:开课管理 ✅
- S08: SEL后端:选课退课+约束 ✅
- S09: GRD后端:成绩录入查询修改 ✅
- S10: LOG后端:操作日志(P1) ✅
- S11: 前端脚手架+路由+登录页 ✅
- S12: 前端:管理员-学生管理 ✅
- S13: 前端:管理员-教师+课程管理 ✅
- S14: 前端:管理员-开课表+成绩管理 ✅

## Remaining Stories: 5

## Codebase Patterns
- Entity: Lombok @Data, @TableName, @TableId(type=AUTO) auto-increment
- Controller: @RestController + @RequiredArgsConstructor, Result<T> wrapper
- Validation: inline checks in controller, return Result.error(code, msg)
- Pagination: MyBatis-Plus Page<T>
- Cascade: CascadeService handles cascading deletes via mapper queries
- JWT: userId/role set as request attributes by JwtFilter
- Tests: @SpringBootTest + @ActiveProfiles("test"), @BeforeEach cleanup, mapper-based
- Service layer: interface + impl pattern; batch ops use @Transactional; joined data built via Map<String,Object>
- Grade patterns: teacher-scoped queries filter by opening->teacher chain; student queries filter by selection->student
- Logging: AOP @Aspect with @Around advice on controller methods; LogAspect extracts userId/role from request attributes; OperationLog entity persisted via OperationLogService; admin-only query via LogController with filtering by operator/operation/targetType/timeRange
- Frontend: Vue3 + Element Plus + Pinia + Vue Router; axios instance with JWT interceptor and 401 auto-logout; role-based route groups (/admin, /teacher, /student); localStorage for token persistence; Layout.vue with sidebar menu per role
- Frontend admin CRUD pattern: api/<resource>.js with CRUD functions; view uses el-card + el-table + el-pagination + el-dialog; search input + add button in card header; edit/add share dialog with isEdit flag; delete with ElMessageBox.confirm showing cascade warning; form validation via el-form rules
- Frontend course pattern: extends CRUD pattern with credit range filters (minCredit/maxCredit query params), el-input-number for credit input with min=1 max=10, custom validator for credit range
- Frontend opening pattern: lookup maps for course/teacher names via computed Map; el-select for course/teacher filtering; add dialog uses select dropdowns; delete with cascade warning
- Frontend grade pattern: read-only detail display in edit dialog (student/course/semester disabled); el-input-number with min=0 max=100 for score; semester filter via computed options from data

## Last Updated: 2026-04-05 S14 complete
