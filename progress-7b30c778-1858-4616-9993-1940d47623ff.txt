# Progress Log - StudentSystem Feature Dev

## Story Results
- **S01: 项目脚手架+数据库Schema** ✅ DONE
- **S02: 后端实体类+Mapper+级联删除** ✅ DONE
- **S03: AUTH后端:统一登录+JWT+修改密码** ✅ DONE
  - JwtUtil: HMAC-SHA token generation/parsing, 2h expiration
  - JwtFilter: OncePerRequestFilter, extracts userId+role from Bearer token, sets SecurityContext
  - AuthService/AuthServiceImpl: login (3 roles: student/teacher/admin), changePassword with BCrypt
  - AuthController: POST /api/auth/login, PUT /api/auth/password
  - SecurityConfig: added JwtFilter before UsernamePasswordAuthenticationFilter
  - Result: unified response wrapper (code/message/data)
  - Login returns 401 with generic message (no info leakage)
  - Password change requires old password, new password min length 6
  - Tests: 8 new tests (studentLogin, teacherLogin, wrongPassword, nonexistentUser, changePassword, shortPassword, wrongOldPassword, jwtClaims) — all pass
  - mvn compile ✅, mvn test ✅ (13 total: 8 auth + 4 cascade + 1 contextLoad)

## Codebase Patterns
- **Entity package:** com.student.entity - @Data Lombok, @TableName, @TableId(type=IdType.AUTO)
- **Mapper package:** com.student.mapper - extends BaseMapper<T>
- **Service package:** com.student.service (interface) + com.student.service.impl (implementation)
- **Controller package:** com.student.controller - @RestController, uses Result<T> wrapper
- **Common package:** com.student.common - Result<T> unified response
- **Util package:** com.student.util - JwtUtil
- **Filter package:** com.student.filter - JwtFilter (OncePerRequestFilter)
- **CascadeService:** @Transactional, uses LambdaQueryWrapper for conditional deletes
- **Config:** SecurityConfig (JwtFilter + BCryptPasswordEncoder + STATELESS), CorsConfig
- **Test:** H2 in-memory DB (MODE=MySQL), schema-h2.sql, spring.sql.init.schema-locations, @ActiveProfiles("test")
- **Test cleanup:** @BeforeEach manually clears all tables via selectList+deleteById
- **Naming:** Entity fields use camelCase (maps to snake_case via mybatis-plus config)
- **Auth errors:** All auth failures throw RuntimeException with code string, controller maps to generic 401

## Remaining Stories: 16
