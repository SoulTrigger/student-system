# Progress Log - StudentSystem Feature Dev

## Story Results
- **S01: 项目脚手架+数据库Schema** ✅ DONE
  - Backend: Spring Boot 3.2.5, port 8082, MyBatis-Plus 3.5.5, JWT, Lombok, H2 for tests
  - Frontend: Vue 3 + Element Plus + Pinia + Axios + ECharts + Vue Router, port 5173
  - schema.sql: 6 tables with FKs, unique constraints, CHECK constraints
  - application.yml: MySQL datasource, port 8082, JWT config (2h expiration)
  - mvn compile ✅, npm run build ✅, mvn test ✅ (1 test passing)

- **S02: 后端实体类+Mapper+级联删除** ✅ DONE
  - 6 Entity classes: Student, Teacher, Course, Opening, Selection, Grade (verified, created in S01)
  - 6 Mapper interfaces: all extend BaseMapper<T> with @Mapper annotation
  - CascadeService interface + CascadeServiceImpl with 4 cascade delete methods
  - Cascade deletion: student→selection+grade; teacher→opening→selection+grade; course→opening→selection+grade; opening→selection+grade
  - Tests: CascadeServiceTest with 4 tests (all pass), H2 in-memory config
  - mvn compile ✅, mvn test ✅ (5 tests: 4 cascade + 1 contextLoad)

## Codebase Patterns
- **Entity package:** com.student.entity - @Data Lombok, @TableName, @TableId(type=IdType.AUTO)
- **Mapper package:** com.student.mapper - extends BaseMapper<T>
- **Service package:** com.student.service (interface) + com.student.service.impl (implementation)
- **CascadeService:** @Transactional, uses LambdaQueryWrapper for conditional deletes
- **Config:** SecurityConfig, CorsConfig in com.student.config
- **Test:** H2 in-memory DB (MODE=MySQL), schema-h2.sql, spring.sql.init.schema-locations
- **Test cleanup:** @BeforeEach manually clears all tables via selectList+deleteById
- **Naming:** Entity fields use camelCase (maps to snake_case via mybatis-plus config)

## Remaining Stories: 17
