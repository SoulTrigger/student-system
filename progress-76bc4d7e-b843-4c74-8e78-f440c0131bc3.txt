# Progress Log
Run: 76bc4d7e-b843-4c74-8e78-f440c0131bc3
Task: Student Information Management System
Started: 2026-04-03T17:08:00+08:00

## Codebase Patterns
- Spring Boot 3.2 + MyBatis-Plus (must use mybatis-plus-spring-boot3-starter, not mybatis-plus-boot-starter)
- Tests use H2 in-memory DB with MODE=MySQL for compatibility
- Package: com.student.system
- Entities in entity/, Mappers in mapper/, Config in config/
- build.sh / test.sh wrap Maven commands
- schema.sql is auto-initialized by Spring Boot sql.init
- Admin user inserted via DataInitializer (CommandLineRunner) using PasswordEncoder, NOT schema.sql
- H2 doesn't support INSERT IGNORE — use DataInitializer for seed data instead
- BCrypt hashes in schema.sql may not match across JVM versions — always use PasswordEncoder at runtime
- JWT: jjwt 0.12.5 with io.jsonwebtoken.security.Keys for HMAC signing
- Auth: interceptor-based (not Spring Security filters), WebMvcConfig registers interceptor excluding /api/auth/**
- RequestContextHolder: ThreadLocal-based, cleared in afterCompletion
- Validation: spring-boot-starter-validation with Jakarta @NotBlank
- Test pattern: @SpringBootTest + @AutoConfigureMockMvc, admin ID fetched dynamically via JdbcTemplate
- Controllers check role via RequestContextHolder.getRole(), return 403 if wrong role
- Service layer throws RuntimeException for business errors, controllers catch and return 400
- DTO pattern: Response DTOs with joined data (studentName, teacherName, courseName from FK lookups)
- Filtering pattern: apply DB-level filters first (via QueryWrapper), then in-memory filtering for cross-table fields
- Student/Teacher tables use auto-increment `id` as PK (not student_id/teacher_id)
- Frontend vitest tests without jsdom: use pure logic tests, avoid mount() unless jsdom env configured

## 2026-04-03 20:42 - US-017: Admin homepage and role-based navigation refinement
- Updated AdminHome.vue with welcome card showing '当前系统：学生信息管理系统', '欢迎您使用本系统', and username
- Refactored Layout.vue admin menus from flat items to sub-menus matching PRD: 学生管理(添加学生/学生列表/搜索学生), 教师管理(添加教师/教师列表), 课程管理(添加课程/搜索课程/开课表管理), 成绩管理(学生成绩查询)
- Fixed teacher menus: 成绩管理 now only has 成绩列表 (was 成绩查询+成绩录入), added 编辑个人信息 label
- Fixed student menus: 选课管理 now only has 已选课程 (was 选课查询+已选课程)
- Added navigation-homepage.test.js with 12 tests covering route guards, menu structure, logout behavior
- All 89 tests pass, build succeeds
- **Files changed:** AdminHome.vue, Layout.vue, navigation-homepage.test.js
- **Learnings:** vitest config has no jsdom environment, so mount() tests fail; use pure logic tests instead
---
